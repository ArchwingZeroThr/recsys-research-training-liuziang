- [x]  完整运行二维二分类代码（或者其他深度学习模型），并保留以下关键结果：
- 部分代码结果如下：
 ![[Pasted image 20260714200120.png]]
![[Pasted image 20260714195547.png]]
- [x]  找到并标注以下关键代码：
  - [x]  数据生成与数据集划分
 ```python
# ---------- 生成原始数据 ----------
num_per_class = 120
generator = torch.Generator().manual_seed(SEED)

class_0 = torch.randn(num_per_class, 2, generator=generator) * 0.70 + torch.tensor([-1.0, -1.0])  # [120, 2]
class_1 = torch.randn(num_per_class, 2, generator=generator) * 0.70 + torch.tensor([1.0, 1.0])    # [120, 2]

#  关键位置1：特征和标签的完整数据集形状
features = torch.cat([class_0, class_1], dim=0).float()   # [240, 2]
labels = torch.cat([torch.zeros(num_per_class), torch.ones(num_per_class)], dim=0).float()  # [240]

# ---------- 划分 train/valid/test ----------
indices = torch.randperm(len(features), generator=torch.Generator().manual_seed(SEED))
train_idx, valid_idx, test_idx = indices[:160], indices[160:200], indices[200:]

train_x, train_y = features[train_idx], labels[train_idx]   # train_x [160,2], train_y [160]
valid_x, valid_y = features[valid_idx], labels[valid_idx]   # valid_x [40,2],  valid_y [40]
test_x,  test_y  = features[test_idx],  labels[test_idx]    # test_x  [40,2],  test_y  [40]
 ```
 ==注：`dim=0` 是**样本维度**（行），拼接后总样本数变为 240，但每个样本的特征数（2维）保持不变，这是为了将所有数据**合并成一个统一的数据集**，方便后续打乱(shuffle)、批处理和输入模型。==
   
  - [x] Tensor与Batch构造
  ```python
BATCH_SIZE = 16

train_set = TensorDataset(train_x, train_y)
valid_set = TensorDataset(valid_x, valid_y)
test_set  = TensorDataset(test_x,  test_y)

train_loader = DataLoader(train_set, BATCH_SIZE, shuffle=True, generator=torch.Generator().manual_seed(SEED))
valid_loader = DataLoader(valid_set, BATCH_SIZE)
test_loader  = DataLoader(test_set,  BATCH_SIZE)

# ---------- 取出一个 batch 预览 ----------
preview_loader = DataLoader(train_set, BATCH_SIZE, shuffle=False)  # 仅用于展示
batch_features, batch_labels = next(iter(preview_loader))

# 关键位置2：一个 batch 的形状
# batch_features -> [16, 2] , batch_labels -> [16]
print("batch_features.shape:", batch_features.shape)  # torch.Size([16, 2])
print("batch_labels.shape:  ", batch_labels.shape)    # torch.Size([16])
  ```
  - [x]  模型定义
  ```python
# 线性层：输入2维特征，输出1个logit
model = nn.Linear(in_features=2, out_features=1)   # 权重形状 [1, 2]，偏置 [1]

# （可选）手动初始化演示
with torch.no_grad():
    model.weight.copy_(torch.tensor([[0.4, -0.2]]))
    model.bias.fill_(0.1)
  ```
  - [x]  Loss定义
  ```python
# 二元交叉熵损失（包含Sigmoid）
loss_fn = nn.BCEWithLogitsLoss()
  ```
  - [x]  Optimizer定义
  ```python
# 随机梯度下降，学习率0.2
optimizer = torch.optim.SGD(model.parameters(), lr=0.2)
  ```
  - [x]  训练循环
  ```python
def train_one_epoch(model, data_loader, optimizer):
    model.train()
    total_loss, total_count = 0.0, 0
    for x, y in data_loader:          # x: [16,2], y: [16]
        optimizer.zero_grad()
        logits = model(x).squeeze(1)  # 关键位置3：模型输出logits形状变为 [16]
        loss = loss_fn(logits, y)     # 标量
        loss.backward()
        optimizer.step()
        total_loss += loss.item() * len(y)
        total_count += len(y)
    return total_loss / total_count

# ---------- 完整训练循环（30个epoch） ----------
model = nn.Linear(2, 1)
optimizer = torch.optim.SGD(model.parameters(), lr=0.2)
best_state, best_valid_loss = None, float("inf")

for epoch in range(1, 31):
    train_loss = train_one_epoch(model, train_loader, optimizer)
    valid_loss, valid_acc = evaluate(model, valid_loader)   # evaluate函数见下一节
    if valid_loss < best_valid_loss:
        best_valid_loss = valid_loss
        best_state = deepcopy(model.state_dict())
    # 打印信息...
  ```
  - [x]  验证与预测
  ```python
def evaluate(model, data_loader):
    model.eval()
    total_loss, total_correct, total_count = 0.0, 0, 0
    with torch.no_grad():
        for x, y in data_loader:
            logits = model(x).squeeze(1)       # [batch_size]
            loss = loss_fn(logits, y).item() * len(y)
            total_loss += loss
            preds = (torch.sigmoid(logits) >= 0.5).float()
            total_correct += (preds == y).sum().item()
            total_count += len(y)
    return total_loss / total_count, total_correct / total_count

# ---------- 测试最佳模型 ----------
model.load_state_dict(best_state)
test_loss, test_acc = evaluate(model, test_loader)
print(f"Test loss: {test_loss:.4f}, Test accuracy: {test_acc:.3f}")
  ```
  - [x]  标出至少三个关键位置的数据shape
	  - `feature`: [240,2]
	  - `batch_features`: [16,2]
	  - `class1`: [120,2]
	  
---
- [x]  结合本次实验，用自己的话解释以下概念：
  - [x]  Tensor
	- 张量本质就是一个多维数组，但此时要注意其维度?（维度和dim的区别是啥？）
	- ![[Pasted image 20260714204404.png]]
  - [x]  Batch
	- Batch就是将很长的一组数据按照Batch_size分割的一小段一小段的数据
  - [x]  Epoch
	- 一组Batch训练完后，叫做完成一个Epoch，一次Epoch中可以有多次数据更新
  - [x]  Logit
	- 用来挤压得到最后一层输出概率
  - [x]  Loss
	- 用于评判模型好与不好的标准
---
- [ ]  按照第一次课的科研记录要求，补充一份实验记录：
  - [ ]  我运行了什么
  - [ ]  我得到了什么结果
  - [ ]  我遇到了什么问题
  - [ ]  我尝试了什么
  - [ ]  我如何判断代码已经跑通
  - [ ]  是否使用了AI/Agent，以及自己核对了哪些内容